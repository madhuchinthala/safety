
// This service is deprecated.
// The application now strictly uses local Regex or the Python backend (services/nlpService.ts) for privacy and offline capability.
// No external GenAI APIs are utilized.

export {};
